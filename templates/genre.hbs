  <li>
    <input type="checkbox" value="None" id="check-{{genre}}" class="genre-btn" name="{{genre}}" / >
    <label for="check-{{genre}}"></label><p>Pop</p>
  </li>
   <li>
    <input type="checkbox" value="None" id="check-{{genre}}" class="genre-btn" name="{{genre}}" / >
    <label for="check-{{genre}}"></label><p>Rock</p>
  </li>
   <li>
    <input type="checkbox" value="None" id="check-{{genre}}" class="genre-btn" name="{{genre}}" / >
    <label for="check-{{genre}}"></label><p>Country</p>
  </li>
   <li>
    <input type="checkbox" value="None" id="check-{{genre}}" class="genre-btn" name="{{genre}}" / >
    <label for="check-{{genre}}"></label><p>Folk</p>
  </li>
 

